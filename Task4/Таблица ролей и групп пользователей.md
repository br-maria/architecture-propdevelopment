# Таблица ролей и групп пользователей Kubernetes (PropDevelopment)

## Цель
Организовать безопасный и управляемый доступ к кластеру Kubernetes в соответствии с ролевой моделью PropDevelopment.  
Настройка выполнена для среды Minikube, отражает боевую структуру ролей и групп пользователей.

---

## 1. Структура ролей и групп

| № | Роль Kubernetes | Назначение | Полномочия (основные действия) | Привязанные группы пользователей |
|---|------------------|------------|--------------------------------|----------------------------------|
| 1 | **viewer** | Только просмотр ресурсов кластера | `get`, `list`, `watch` для всех namespace | `dev-viewers` — разработчики, тестировщики, аналитики |
| 2 | **operator** | Настройка и управление ресурсами приложений | `get`, `list`, `watch`, `create`, `update`, `delete` для Pod, Deployment, Service, ConfigMap, Secret | `dev-operators` — инженеры DevOps, CI/CD |
| 3 | **admin** | Полный доступ в пределах namespace | Все действия (`*`) над всеми ресурсами в конкретном namespace | `ns-admins` — тимлиды продуктовых команд |
| 4 | **cluster-admin** | Полный доступ к кластеру, включая системные ресурсы | Все действия (`*`) над всеми объектами Kubernetes, включая RBAC, CRD, Node | `infosec`, `devops-leads`, `it-admins` |
| 5 | **readonly-auditor** | Аудит и мониторинг безопасности | `get`, `list` для Secret, Pod, RoleBinding, AuditLog, Event | `security-auditors` — специалисты по ИБ |

---

## 2. Проверка работы ролей

После применения YAML-манифестов и создания пользователей были выполнены тесты:

```bash
kubectl auth can-i get pods --as viewer1       # ✅ yes
kubectl auth can-i delete pods --as viewer1    # 🚫 no
kubectl auth can-i create deployments --as operator1  # ✅ yes
